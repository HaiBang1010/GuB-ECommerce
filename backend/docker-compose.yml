services:
  db:
    image: postgres:16
    container_name: gub-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: gub
      POSTGRES_PASSWORD: gub
      POSTGRES_DB: gub
    ports:
      - "25432:5432"
    volumes:
      - gub-pgdata:/var/lib/postgresql/data
volumes:
  gub-pgdata: